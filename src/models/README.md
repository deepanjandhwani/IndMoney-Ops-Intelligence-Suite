# Models

Shared TypeScript types and domain constants live here.

Database schemas are added in Phase 1 through Supabase migrations; this folder holds application-level types that mirror those persisted models.
