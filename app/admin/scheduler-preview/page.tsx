import { ModulePlaceholder } from "@/ui/ModulePlaceholder";

export default function SchedulerPreviewPage() {
  return (
    <ModulePlaceholder
      role="Admin"
      title="Advisor Scheduler Preview"
      description="Admin preview for scheduler flows will arrive with the shared booking state machine in Phase 5."
    />
  );
}
