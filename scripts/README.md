# Scripts

Automation entry points live here, including future ingestion, clustering, RAG source refresh, and eval report scripts.

Scripts must read configuration from `.env`/GitHub Actions secrets documented in `.env.example`.
